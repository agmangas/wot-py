version: 2

sphinx:
  builder: html
  configuration: docs/conf.py

build:
  image: latest
      
python:
  version: 3.7
  install:
    - method: pip
      path: .
      extra_requirements:
        - tests
  system_packages: true
 